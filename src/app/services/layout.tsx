export const metadata = {
  title: "Services | MD. Abu Hanif",
  description: "Professional web development services I offer, including frontend, backend, and full-stack development.",
};

export default function ServicesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 