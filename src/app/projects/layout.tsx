export const metadata = {
  title: "Projects | MD. Abu Hanif",
  description: "Explore my portfolio of web development projects, applications and code samples.",
};

export default function ProjectsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 